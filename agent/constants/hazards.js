const HAZARD_BLOCK_TAGS = ['lava', 'fire', 'magma', 'cactus', 'berry_bush', 'campfire', 'powder_snow']

const DEFAULT_HAZARD_RADIUS = 4
const SAFE_PATH_BUFFER_RADIUS = 6

module.exports = {
  HAZARD_BLOCK_TAGS,
  DEFAULT_HAZARD_RADIUS,
  SAFE_PATH_BUFFER_RADIUS
}
